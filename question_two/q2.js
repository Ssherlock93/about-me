function multiply(a,b) {
  var product = a * b;
  var arr = [];
  arr.push(product);
  var message = (a * b, + 'The Product of' + a + 'and' + b + 'is' + product + '.');
  arr.push(message);
  return arr;



}
testmultiply(5,9);